# PK Sompura — front end

Vite 7 + React 19.

```bash
npm run dev      # http://localhost:5173
npm run build    # -> dist/
npm run lint
```

`VITE_*` values are inlined at build time and are therefore public — nothing
secret may be named `VITE_*`. Hosting and the proxy: `../DEPLOYMENT.md`.
Palette, contrast and landmines: `../design-system/pk-sompura/MASTER.md`.
